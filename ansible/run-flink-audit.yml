---
- name: run the flik audit job
  hosts: "{{ host }}"
  become: yes
  tasks:
  - shell:
      cmd: /mount/data/analytics/scripts/flink_audit-latest/flink-audit-new.sh

# - name: Verify the status of the report execution
#   hosts: "{{ host }}"
#   become: yes
#   tasks: 
#   - shell:
#       cmd: cat /mount/data/analytics/scripts/logs/joblog.log | grep -i "completed execution"
#     register: report_status

#   - debug:
#       var: report_status
